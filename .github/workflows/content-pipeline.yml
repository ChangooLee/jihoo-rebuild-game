name: Content Pipeline (Weekly)

on:
  schedule:
    - cron: '0 9 * * 0'  # 매주 일요일 18:00 KST (UTC+9)
  workflow_dispatch:  # 수동 실행도 가능

jobs:
  generate-content:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: tools/requirements.txt
      
      - name: Install Python dependencies
        run: |
          cd tools
          pip install -r requirements.txt
      
      - name: Generate content (all subjects)
        run: |
          cd tools
          # 주차별 시드 오프셋
          SEED_OFFSET=$(($(date +%W) * 30))
          echo "Week: $(date +%W), Offset: $SEED_OFFSET"
          
          # 과목별 생성 (40:30:15:15 비율 목표)
          python3 generators/math/build_bank.py --seeds 40 --offset $SEED_OFFSET
          python3 generators/english/build_bank.py --seeds 30 --offset $SEED_OFFSET
          python3 generators/science/build_bank.py --seeds 15 --offset $SEED_OFFSET
          python3 generators/social/build_bank.py --seeds 15 --offset $SEED_OFFSET
      
      - name: Validate generated content
        run: |
          node tools/builder/validate-simple.mjs
      
      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v5
        with:
          commit-message: 'chore(content): weekly content refresh'
          branch: chore/content-auto-update
          title: '🤖 Weekly Content Update'
          body: |
            ## 자동 생성된 콘텐츠
            
            - 수학 문항 자동 생성 완료
            - 스키마 검증 통과
            
            **병합 전 확인사항:**
            - [ ] 문항 품질 확인
            - [ ] 난이도 분포 적절
          labels: content, automated

