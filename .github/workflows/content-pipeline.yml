name: Content Pipeline (Weekly)

# 주석 처리 - 필요시 활성화
# on:
#   schedule:
#     - cron: '0 18 * * 0'  # 매주 일요일 18:00 KST
#   workflow_dispatch:  # 수동 실행 가능

on:
  workflow_dispatch:  # 수동 실행만 가능

jobs:
  generate-content:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: tools/requirements.txt
      
      - name: Install Python dependencies
        run: |
          cd tools
          pip install -r requirements.txt
      
      - name: Generate math content
        run: |
          cd tools
          python3 generators/math/build_bank.py --seeds 100
      
      - name: Validate generated content
        run: |
          node tools/builder/validate.mjs
      
      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v5
        with:
          commit-message: 'chore(content): weekly content refresh'
          branch: chore/content-auto-update
          title: '🤖 Weekly Content Update'
          body: |
            ## 자동 생성된 콘텐츠
            
            - 수학 문항 자동 생성 완료
            - 스키마 검증 통과
            
            **병합 전 확인사항:**
            - [ ] 문항 품질 확인
            - [ ] 난이도 분포 적절
          labels: content, automated

